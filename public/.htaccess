# Proteção quando public/ é implantado como document root Apache.
<FilesMatch "\.(?:db|sqlite|sqlite3)(?:-(?:wal|shm|journal))?$">
    Require all denied
</FilesMatch>
<FilesMatch "^(?i:SECRETS)$">
    Require all denied
</FilesMatch>
